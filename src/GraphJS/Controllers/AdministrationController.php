<?php

/*
 * This file is part of the Pho package.
 *
 * (c) Emre Sokullu <emre@phonetworks.org>
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */

 namespace GraphJS\Controllers;

use Psr\Http\Message\ResponseInterface;
use Psr\Http\Message\ServerRequestInterface;
use Pho\Kernel\Kernel;
use PhoNetworksAutogenerated\User;
use PhoNetworksAutogenerated\UserOut\Star;
use PhoNetworksAutogenerated\UserOut\Comment;
use Pho\Lib\Graph\ID;
use PhoNetworksAutogenerated\PrivateContent;
use Stringy\StaticStringy as S;
use GraphJS\Utils;


/**
 * Administrative calls go here.
 * 
 * @author Emre Sokullu <emre@phonetworks.org>
 */
class AdministrationController extends AbstractController
{

    /**
     * SuperAdmin Hash
     * 
     * Generated randomly by a uuidgen
     *
     * @var string
     */
    protected $superadmin_hash = ""; // not a good idea, since it's public

    protected function requireAdministrativeRights(ServerRequestInterface $request, ResponseInterface $response): bool
    {
        //$founder = $this->kernel->founder();
        //$hash = md5(strtolower(sprintf("%s:%s", $founder->getEmail(), $founder->getPassword())));
        $hash = md5(getenv("FOUNDER_PASSWORD"));
        error_log("founder password is: ".getenv("FOUNDER_PASSWORD"));
        error_log("hash is: ".$hash);
        $data = $request->getQueryParams();
        $validation = $this->validator->validate($data, [
            "hash" => "required"
        ]);
        //$v->rule('length', [['hash', 32]]);
        //error_log($founder->getEmail().":".$founder->getPassword().":".$hash);
        error_log("data hash is: ".$data["hash"]);
        if($validation->fails()||($data["hash"]!=$hash&&$data["hash"]!=$this->superadmin_hash)) {
            return false;
        }
        return true;
    }

    protected function _getPendingComments(): array
    {
        $pending_comments = [];
        $res = $this->kernel->index()->query("MATCH (a:user)-[e:comment {Pending: true}]-(n:page) RETURN a.udid AS author_id, a.Email AS author_email, n.udid AS page_id, e.udid AS comment_id, n.Url AS page_url, n.Title AS page_title, e.Content AS comment");
        $array = $res->results();
        foreach($array as $a) {
            $pending_comments[] = [
                "comment_id" => $a["comment_id"],
                "author_id" => $a["author_id"], 
                "author_email" => $a["author_email"], 
                "page_id" => $a["page_id"], 
                "page_url" => $a["page_url"],
                "page_title" => $a["page_title"],
                "comment" => $a["comment"]
            ];
        }
        return $pending_comments;
    }

    public function getPendingComments(ServerRequestInterface $request, ResponseInterface $response)
    {
        if(!$this->requireAdministrativeRights(...\func_get_args()))
            return $this->failHash($response);
        $is_moderated = ($this->kernel->graph()->getCommentsModerated() === true);
        /*if(!$is_moderated)
            return $this->fail($response);
        */
        $pending_comments = $this->_getPendingComments();
        return $this->succeed($response, ["pending_comments"=>$pending_comments]);
    }

    /**
     * @todo Check for admin capabilities
     */
    public function approvePendingComment(ServerRequestInterface $request, ResponseInterface $response)
    {
        if(!$this->requireAdministrativeRights(...\func_get_args()))
            return $this->failHash($response);
        $data = $request->getQueryParams();
        $validation = $this->validator->validate($data, [
            "comment_id" => "required"
        ]);
        if($validation->fails()) {
            return $this->fail($response, "comment_id required");
        }
        try {
            $comment = $this->kernel->gs()->edge($data["comment_id"]);
        }
        catch(\Exception $e) {
            return $this->fail($response, "Invalid Comment ID.");
        }
        if(!$comment instanceof Comment)
            return $this->fail($response, "Invalid Comment.");
        $comment->setPending(false);
        return $this->succeed($response);
    }

    public function setBlogEditor(ServerRequestInterface $request, ResponseInterface $response)
    {
        if(!$this->requireAdministrativeRights(...\func_get_args()))
            return $this->failHash($response);

            $data = $request->getQueryParams();
            $validation = $this->validator->validate($data, [
                "user_id" => "required",
            ]);
            if($validation->fails()) {
                return $this->fail($response, "A user id  is required");
            }
            $is_editor = isset($data["is_editor"]) && (bool) $data["is_editor"];
            try {
                $user = $this->kernel->gs()->node($data["user_id"]);
            }
            catch(\Exception $e) {
                return $this->fail($response, "Invalid user id");
            }
            $user->setIsEditor((bool) ($is_editor));
            return $this->succeed($response);
    }

    public function setCustomFields(ServerRequestInterface $request, ResponseInterface $response)
    {
        if(!$this->requireAdministrativeRights(...\func_get_args()))
            return $this->failHash($response);

        // only works with Graph.js
        if(!$this->kernel->graph() instanceof \PhoNetworksAutogenerated\Site) {
            return $this->fail($response, "Invalid function");
        }

        $data = $request->getQueryParams();
        $custom_field1 = ((isset($data["custom_field1"])) ? (string) $data["custom_field1"] : "");
        $custom_field1_must = ((isset($data["custom_field1_must"])) ? (bool) $data["custom_field1_must"] : false);
        $custom_field2 = ((isset($data["custom_field2"])) ? (string) $data["custom_field2"] : "");
        $custom_field2_must = ((isset($data["custom_field2_must"])) ? (bool) $data["custom_field2_must"] : false);
        $custom_field3 = ((isset($data["custom_field3"])) ? (string) $data["custom_field3"] : "");
        $custom_field3_must = ((isset($data["custom_field3_must"])) ? (bool) $data["custom_field3_must"] : false);

        $this->kernel->graph()->setCustomField1($custom_field1);
        $this->kernel->graph()->setCustomField1Must($custom_field1_must);
        $this->kernel->graph()->setCustomField2($custom_field2);
        $this->kernel->graph()->setCustomField2Must($custom_field2_must);
        $this->kernel->graph()->setCustomField3($custom_field3);
        $this->kernel->graph()->setCustomField3Must ($custom_field3_must);
        
        $this->kernel->graph()->persist();
        return $this->succeed($response);
    }

    public function getCustomFields(ServerRequestInterface $request, ResponseInterface $response)
    {
        /*
        if(!$this->requireAdministrativeRights(...\func_get_args()))
            return $this->failHash($response);
        */
        
        // only works with Graph.js
        if(!$this->kernel->graph() instanceof \PhoNetworksAutogenerated\Site) {
            return $this->fail($response, "Invalid function");
        }

        $custom_field1 = (string) $this->kernel->graph()->attributes()->CustomField1;
        $custom_field2 = (string) $this->kernel->graph()->attributes()->CustomField2;
        $custom_field3 = (string) $this->kernel->graph()->attributes()->CustomField3;

        $custom_field1_must = (string) $this->kernel->graph()->attributes()->CustomField1Must;
        $custom_field2_must = (string) $this->kernel->graph()->attributes()->CustomField2Must;
        $custom_field3_must = (string) $this->kernel->graph()->attributes()->CustomField3Must;
        
        return $this->succeed($response, [
            "custom_field1"=>$custom_field1,
            "custom_field2"=>$custom_field2,
            "custom_field3"=>$custom_field3,
            "custom_field1_must"=>$custom_field1_must,
            "custom_field2_must"=>$custom_field2_must,
            "custom_field3_must"=>$custom_field3_must,
        ]);
    }

    public function setAbout(ServerRequestInterface $request, ResponseInterface $response)
    {
        if(!$this->requireAdministrativeRights(...\func_get_args()))
            return $this->failHash($response);
        $data = $request->getQueryParams();
        $validation = $this->validator->validate($data, [
            "txt" => "required"
        ]);
        if($validation->fails()) {
            return $this->fail($response, "About txt is required");
        }
        $about = (string) $data["txt"];
        $graph = $this->kernel->graph();
        if(!$graph instanceof \PhoNetworksAutogenerated\Network) {
            return $this->fail($response, "Invalid function");
        }
        $this->kernel->graph()->setAbout($about);
        $this->kernel->graph()->persist();
        return $this->succeed($response);
    }

    public function getAbout(ServerRequestInterface $request, ResponseInterface $response)
    {
        $graph = $this->kernel->graph();
        if(!$graph instanceof \PhoNetworksAutogenerated\Network) {
            return $this->fail($response, "Invalid function");
        }
        $about = $this->kernel->graph()->getAbout();
        return $this->succeed($response, [
            "about" => $about
        ]);
    }

    public function setCommentModeration(ServerRequestInterface $request, ResponseInterface $response)
    {
        if(!$this->requireAdministrativeRights(...\func_get_args()))
            return $this->failHash($response);
        $data = $request->getQueryParams();
        $validation = $this->validator->validate($data, [
            "moderated" => "required"
        ]);
        //$v->rule('boolean', ['moderated']);
        if($validation->fails()) {
            return $this->fail($response, "A boolean 'moderated' field is required");
        }
        $is_moderated = (bool) $data["moderated"];
        if(!$is_moderated) {
            $pending_comments = $this->_getPendingComments();
            foreach($pending_comments as $c) {
                try {
                    $comment = $this->kernel->gs()->edge($c["comment_id"]);
                    $comment->setPending(false);
                }
                catch (\Exception $e) {
                    error_log("a-oh can't fetch comment id ".$c["comment_id"]);
                }
            }
        }
        $this->kernel->graph()->setCommentsModerated($is_moderated);
        $this->kernel->graph()->persist();
        return $this->succeed($response);
    }

    public function getCommentModeration(ServerRequestInterface $request, ResponseInterface $response)
    {
        if(!$this->requireAdministrativeRights(...\func_get_args()))
            return $this->failHash($response);
        $is_moderated = (bool) $this->kernel->graph()->getCommentsModerated();
        return $this->succeed($response, ["is_moderated"=>$is_moderated]);
    }

    public function deletePendingComment(ServerRequestInterface $request, ResponseInterface $response)
    {
        if(!$this->requireAdministrativeRights(...\func_get_args()))
            return $this->failHash($response);
        $data = $request->getQueryParams();
        $validation = $this->validator->validate($data, [
            "comment_id" => "required"
        ]);
        if($validation->fails()) {
            return $this->fail($response, "comment_id required");
        }
        try {
            $comment = $this->kernel->gs()->edge($data["comment_id"]);
        }
        catch(\Exception $e) {
            return $this->fail($response, "Invalid Comment ID.");
        }
        if(!$comment instanceof Comment)
            return $this->fail($response, "Invalid Comment.");
        $comment->destroy();
        return $this->succeed($response);
    }

    public function getObjectCounts(ServerRequestInterface $request, ResponseInterface $response)
    {
        $nodes_count = $edges_count = $actors_count = 0;
        $entities = $this->kernel->database()->keys('*'); // because it's faster than ->members()
        foreach($entities as $entity) {
            $clue = \hexdec($entity[0]);
            if($clue==4) $actors_count++;
            if($clue < 6) $nodes_count++;
            else $edges_count++;
        }
        return $this->succeed($response, [
            "actor_count" => (string) $actors_count,
            "node_count"  => (string) $nodes_count,
            "edge_count"  => (string) $edges_count
        ]);
    }

    public function setFounderPassword(ServerRequestInterface $request, ResponseInterface $response)
    {
        if(!$this->requireAdministrativeRights(...\func_get_args()))
            return $this->failHash($response);
        $data = $request->getQueryParams();
        $validation = $this->validator->validate($data, [
            "password" => "required"
        ]);
        if($validation->fails()) {
            return $this->fail($response, "password required");
        }
        if(!$this->checkPasswordFormat($data["password"])) {
            return $this->fail($response, "password format not good");
        }
        $founder = $this->kernel->founder();
        $founder->setPassword($data["password"]);
        $founder->persist();
        return $this->succeed($response);
    }
 
         public function deleteMember(ServerRequestInterface $request, ResponseInterface $response)
      {
        if(!$this->requireAdministrativeRights(...\func_get_args())) {
            return $this->failHash($response);
        }
        $data = $request->getQueryParams();
        $validation = $this->validator->validate($data, [
            "id" => "required"
        ]);
        if($validation->fails()) {
            return $this->fail($response, "User ID unavailable.");
        }
        try {
            $entity = $this->kernel->gs()->entity($data["id"]);
        }
        catch(\Exception $e) {
            return $this->fail($response, "No such Entity");
        }
        if($entity instanceof User) {
            try {
                $entity->destroy();
            }
            catch(\Exception $e) {
                return $this->fail($response, "Problem with deleting the User");
            }
            return $this->succeed($response);
        }
        return $this->fail($response, "The ID does not belong to a User.");
    }
 
    public function getId(ServerRequestInterface $request, ResponseInterface $response)
    {
      $data = $request->getQueryParams();
      $validation = $this->validator->validate($data, [
          "ref" => "required"
      ]);
      if($validation->fails()) {
          return $this->fail($response, "User ID unavailable.");
      }
      $clean_ref = \str_replace(['"',"'","\\"], "", $data["ref"]);
      $res = $this->kernel->index()->query("MATCH (a:user {Username: \"".$clean_ref."\"}) RETURN a.udid AS author_id");
        $array = $res->results();
        if(count($array)==1)
            return $this->succeed($response, ["id"=>$array[0]["author_id"]]);
        $res = $this->kernel->index()->query("MATCH (a:group {Title: \"".$clean_ref."\"}) RETURN a.udid AS group_id");
            $array = $res->results();
            if(count($array)==1)
                return $this->succeed($response, ["id"=>$array[0]["group_id"]]);
        $res = $this->kernel->index()->query("MATCH (a:group {Title: \"".\str_replace("_"," ", $clean_ref)."\"}) RETURN a.udid AS group_id");
                $array = $res->results();
                if(count($array)==1)
                    return $this->succeed($response, ["id"=>$array[0]["group_id"]]);

        return $this->fail($response, "No such user or group");
            
  }

  public function approveMembership(ServerRequestInterface $request, ResponseInterface $response)
  {
    if(!$this->requireAdministrativeRights(...\func_get_args())) {
        return $this->failHash($response);
    }

    if(!$this->isMembershipModerated()) {
        return $this->fail($response, "Membership not moderated");
    }

    $data = $request->getQueryParams();
      $validation = $this->validator->validate($data, [
          "member_id" => "required"
      ]);
      if($validation->fails()) {
          return $this->fail($response, "Member ID is required.");
      }

      try {
        $entity = $this->kernel->gs()->entity($data["member_id"]);
    }
    catch(\Exception $e) {
        return $this->fail($response, "No such Entity");
    }
    if($entity instanceof User) {
        try {
            $entity->setPending(false);
        }
        catch(\Exception $e) {
            return $this->fail($response, "Problem with approving the User");
        }
        return $this->succeed($response);
    }
    return $this->fail($response, "Problem approving member")    ;
  }

  public function getPendingMemberships(ServerRequestInterface $request, ResponseInterface $response)
  {
    if(!$this->requireAdministrativeRights(...\func_get_args())) {
        return $this->failHash($response);
    }
        if(!$this->isMembershipModerated())
            return $this->succeed($response, ["members"=>[]]);

        $verificationRequired = $this->isVerificationRequired();
        $nodes = $this->kernel->graph()->members();
        $members = [];
        foreach($nodes as $node) {
            //if($node instanceof User) error_log(print_r($node->toArray()));
            if($node instanceof User && 
                    isset($node->attributes()->Pending) && $node->getPending() &&
                    (!$verificationRequired||!$node->attributes()->PendingVerification)
                    ) {
                    $members[(string) $node->id()] = [
                        "id" => (string) $node->id(),
                        "username" => (string) $node->getUsername(),
                        "email" => (string) $node->getEmail(),
                        "avatar" => (string) $node->getAvatar(),
                        "signup_time" =>  Utils::time_elapsed_string(date("r", $node->getJoinTime()))
                    ];
                }
            }
        error_log(count($members));
        return $this->succeed($response, ["members" => $members]);
    
  }

  public function setMembershipModerationMode(ServerRequestInterface $request, ResponseInterface $response)
  {
    if(!$this->requireAdministrativeRights(...\func_get_args())) {
        return $this->failHash($response);
    }
    $data = $request->getQueryParams();
      $validation = $this->validator->validate($data, [
          "mode" => "required"
      ]);
      if($validation->fails()) {
          return $this->fail($response, "Mode is required.");
      }

      $mode = (($data["mode"]=="off") ? false : (bool) $data["mode"]);

      $this->kernel->graph()->setMembershipModerated($mode);
      $this->kernel->graph()->persist();
      return $this->succeed($response);
    
  }

  public function getMembershipModerationMode(ServerRequestInterface $request, ResponseInterface $response)
  {
    $mode = $this->isMembershipModerated();

    return $this->succeed($response,
        ["mode" => intval($mode)]
    );
    
  }

  public function setVerificationRequiredMode(ServerRequestInterface $request, ResponseInterface $response)
  {
    if(!$this->requireAdministrativeRights(...\func_get_args())) {
        return $this->failHash($response);
    }
    $data = $request->getQueryParams();
      $validation = $this->validator->validate($data, [
          "mode" => "required"
      ]);
      if($validation->fails()) {
          return $this->fail($response, "Mode is required.");
      }

      $mode = (bool) $data["mode"];

      $this->kernel->graph()->setVerificationRequired($mode);

      return $this->succeed($response);
    
  }

  public function getVerificationRequiredMode(ServerRequestInterface $request, ResponseInterface $response)
  {
    $mode = $this->isVerificationRequired();

    return $this->succeed($response,
        ["mode" => intval($mode)]
    );
    
  }

  public function setReadOnlyMode(ServerRequestInterface $request, ResponseInterface $response)
  {
    if(!$this->requireAdministrativeRights(...\func_get_args())) {
        return $this->failHash($response);
    }
    $data = $request->getQueryParams();
      $validation = $this->validator->validate($data, [
          "mode" => "required"
      ]);
      if($validation->fails()) {
          return $this->fail($response, "Mode is required.");
      }

      $mode = (bool) $data["mode"];

      $this->kernel->graph()->setReadOnly($mode);

      return $this->succeed($response);
    
  }

  public function getReadOnlyMode(ServerRequestInterface $request, ResponseInterface $response)
  {
    $mode = $this->isReadOnly();

    return $this->succeed($response,
        ["mode" => intval($mode)]
    );
    
  }

  public function setAllModes(ServerRequestInterface $request, ResponseInterface $response)
  {
    if(!$this->requireAdministrativeRights(...\func_get_args())) {
        return $this->failHash($response);
    }
    $data = $request->getQueryParams();
      $validation = $this->validator->validate($data, [
          "read_only" => "required",
          "membership_moderated" => "required",
          "verification_required" => "required"
      ]);
      if($validation->fails()) {
          return $this->fail($response, "read_only, membership_moderated, verification_required required.");
      }

      $vars = ["read_only",  "membership_moderated", "verification_required"];
      foreach($vars as $var) {
        $$var = ($data[$var]==="off"||$data[$var]==="false") ? false : (bool) $data[$var];
        $func = sprintf("set%s", S::upperCamelize($var));
        $getter = sprintf("get%s", S::upperCamelize($var));
        error_log($func);
        error_log(gettype($$var));
        error_log(intval($$var));
        error_log(intval($this->kernel->graph()->$getter()));
        $this->kernel->graph()->$func($$var);
        error_log(intval($this->kernel->graph()->$getter()));
      }
      $this->kernel->graph()->persist();

      return $this->succeed($response);
    
  }

  public function getAllModes(ServerRequestInterface $request, ResponseInterface $response)
  {
    $read_only = $this->isReadOnly();
    $membership_moderated = $this->isMembershipModerated();
    $verification_required = $this->isVerificationRequired();

    return $this->succeed($response,
        [
            "read_only"             => intval($read_only),
            "membership_moderated"  => intval($membership_moderated),
            "verification_required" => intval($verification_required)
        ]
    );
    
  }

  public function getSingleSignonKey(ServerRequestInterface $request, ResponseInterface $response)
    {
        if(!$this->requireAdministrativeRights(...\func_get_args())) {
            return $this->failHash($response);
        }
        $key = getenv("SINGLE_SIGNON_TOKEN_KEY") ? getenv("SINGLE_SIGNON_TOKEN_KEY") : "";
        if(empty($key)) {
            return $this->fail($response, "No single sign-on key");
        }
        return $this->succeed($response, ["key"=>$key]);
    }

}
